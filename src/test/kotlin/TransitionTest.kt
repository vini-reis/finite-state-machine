import org.junit.jupiter.api.Timeout
import java.util.concurrent.TimeUnit

@Timeout(30, unit = TimeUnit.SECONDS)
class TransitionTest {
    /**
     * TODO: Create the below tests:
     *      - Validate an entire flow simulating TDD
     *      - Validate FSM reset
     */

}